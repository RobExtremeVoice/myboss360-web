# Providers

Application providers live here.

Use this folder for React context providers, app-level dependency wiring, session providers, feature-flag providers, and client-side environment boundaries.
