# Shared Components

This folder contains higher-level reusable components shared across domains.

Use it when a component is more specific than a UI primitive but still useful to multiple product areas.
