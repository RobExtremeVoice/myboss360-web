# Layout Components

This folder contains reusable layout structure.

Use it for headers, footers, sidebars, shells, page containers, section wrappers, and other structural components that organize screens without owning business logic.
