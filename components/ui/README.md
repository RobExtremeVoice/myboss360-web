# UI Components

This folder contains low-level reusable UI primitives.

Use this area for buttons, inputs, cards, dialogs, tables, badges, and other styling-aware building blocks that can be shared across the full product.
