import { PlaceholderPage } from "@/components/dashboard/PlaceholderPage";

export default function RegisterPage() {
  return (
    <PlaceholderPage
      title="Register"
      description="This route is reserved for future account creation and onboarding. It currently exists as a production-ready placeholder inside the new auth layout."
    />
  );
}
