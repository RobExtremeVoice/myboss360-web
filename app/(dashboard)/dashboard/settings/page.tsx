import { PlaceholderPage } from "@/components/dashboard/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="The Settings module will contain future workspace configuration, preferences, permissions, and account management controls."
    />
  );
}
