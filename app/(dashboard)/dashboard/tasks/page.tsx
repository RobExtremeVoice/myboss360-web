import { PlaceholderPage } from "@/components/dashboard/PlaceholderPage";

export default function TasksPage() {
  return (
    <PlaceholderPage
      title="Tasks"
      description="The Tasks module will be used for ownership, follow-through, task prioritization, and accountability across the workspace."
    />
  );
}
