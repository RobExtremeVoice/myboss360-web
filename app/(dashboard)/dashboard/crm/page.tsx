import { PlaceholderPage } from "@/components/dashboard/PlaceholderPage";

export default function CrmPage() {
  return (
    <PlaceholderPage
      title="CRM"
      description="The CRM module will centralize contacts, companies, deals, and customer relationship workflows inside the authenticated application."
    />
  );
}
