import { PlaceholderPage } from "@/components/dashboard/PlaceholderPage";

export default function CalendarPage() {
  return (
    <PlaceholderPage
      title="Calendar"
      description="The Calendar module will support scheduling, event context, meeting planning, and timeline visibility across teams."
    />
  );
}
