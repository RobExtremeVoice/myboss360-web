# Config

Central application configuration lives here.

Use this folder for route definitions, navigation metadata, feature flags, and other static configuration that helps keep behavior predictable and scalable.
