# Constants

Static application constants live here.

Use this folder for non-secret fixed values such as labels, limits, status maps, defaults, and cross-module configuration values that should not be embedded inside components.
