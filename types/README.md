# Types

Shared TypeScript types and interfaces live here.

Use this folder for domain models, API contracts, shared enums, and cross-module type exports that should stay framework-agnostic whenever possible.
